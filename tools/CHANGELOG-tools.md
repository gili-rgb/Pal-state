# CHANGELOG — tools/

יומן שינויים לסקריפטים ב-`tools/` בלבד: preflight, postflight, pal_lint,
ledger_lint, ledger_patch, ledger_merge, version_guard והבדיקות שלהם.

**החלטה (2026-08-13):** תיעוד שינויים בכלים חי מהיום כאן, ולא בתוך
`skills/content-machine/SKILL.md`. הסיבה מכנית: SKILL.md הוא 71KB ו-CHANGELOG.md
הוא 30KB, וכל דחיפה שלהם דרך MCP מחייבת שליחת הקובץ המלא. שינוי של שלוש שורות
בכלי גרר עלות של קובץ שלם. זו תחילת פיצול ניהול הגרסאות לקבצים קטנים ייעודיים:
שינוי בכלי נוגע רק בקובץ הזה, וגרסת content-machine אינה זזה בגללו.

---

> **2026-08-16 — D2 שלב ד: רשימת המותגים של מרום**

**קבצי סקיל בלבד, אין שינוי בכלים.** `project-marom.md` סעיף "מותגים" נבנה
מחדש ממיפוי GSC חי. הרשימה הקודמת מנתה שמונה מותגים בזמן שלמרום תנועה על
**למעלה מעשרים**, וזו חלק מהסיבה שגרונדיג, אינדסיט, אמאנה, מולינקס ומג'ימיקס
יושבים על אפס: `preflight` מסמן אותם כדורשים הזנת קישורים בזמן שקובץ הפרויקט
אמר שהם מחוץ לתחום.

**הכרעת גיל:** קיומו של עמוד מותג אינו תנאי לכתיבה. מותג עם תוכן טוב וחשיפות
מותר גם בלי עמוד `/brands/`. סמוראי (13,416 חשיפות, בלי עמוד מותג) הוא הדוגמה.
ההמלצה המקורית שלי, "עמוד מותג = מותר לכתוב", נדחתה ובצדק: מה שקובע הוא
ההחרגות, לא מלאי העמודים.

**ההחרגות נכתבו לצד הרשימה** במקום להיסמך על הזיכרון: בקו אסורה לחלוטין
למרות 12,131 חשיפות ו-794 קליקים, דלונגי בלי מקררים ובלי claim של "שירות
רשמי" למכונות קפה, מוצרים קטנים במעבדה בלבד, ווייקינג ופיאבה בלי מחיר.
**חשיפות לבדן אינן מתירות כתיבה.**

**תיקון בתוך אותו סבב, אחרי שגיל ערער על שתי טענות:** (1) טענתי שדלונגי
מפוצל בין שני slugs. **שגוי.** אין `/product-category/delonghi/` כלל, וה-3,177
שייחסתי לו הגיעו מ-`/brands/delonghi-service/` שמופיע בדאטה כשבע שורות
נפרדות. שגיאת אגרגציה: ספרתי עמוד מותג כעמוד קטגוריה. (2) הכנסתי את הייסנס
לרשימת המותגים. **שגוי.** `/product-category/hisenes/` הוא קטגוריית שלטים
לטלוויזיות, ומרום אינה נותנת שירות להייסנס. אותו דבר ל-TCL. שניהם עברו יחד
עם בוס ו-JBL לשכבת "אלקטרוניקה, חלפים בלבד". **TCL כן מותג שירות של פלרום
(340 ₪) ולא של מרום** — סיכון גלישה בין אתרים, נרשם במפורש.

**ממצא שנשאר:** `/product-category/hisenes/` מדורג על "שירות הייסנס"
(מיקום 12.5), "הייסנס שירות" (13.2) ו"שירות למקרר הייסנס" (15.1). שאילתות
שירות שמרום אינה יכולה לשרת. לא לחזק אותן בתוכן.

**אושר על ידי גיל:** קראון, קינג ולה קאסה הם מוצרי חשמל בשירות מרום.
אומת גם בדאטה: מכונות כביסה, תנורים, מדיחים ומייבשים.

---

> **2026-08-16 — D2 שלב ג: סגירת ה-placeholders הפתוחים**

**`version_guard.py` v1.2 → v1.3 — `OPEN_PLACEHOLDER` (ERROR)**
נופל על `[גיל:` בכל קובץ `.md` בתיקיית הסקיל, למעט CHANGELOG.
הרקע: `project-marom.md` החזיק שני שדות ממוענים לגיל, `sameAs` והוכחת ניסיון,
מ-5 ביולי עד 16 באוגוסט. הקובץ נקרא על ידי המכונה כמקור `verbatim`, אף שער
לא ראה את השדות, ושום דוח לא הציג אותם. **placeholder בקובץ מקור אמת הוא
חוב שקט.** מעכשיו: או שהנתון מתקבל ונכתב, או שהיעדרו נרשם כהחלטה מפורשת.

**הכרעת גיל:** אלה כל הנתונים שקיימים על מיכה איתן. שני השדות נסגרו כהחלטה.
`sameAs` מושמט לגמרי מישות ה-`Person`. **מרום שומרת ישות `Person` ופלרום לא,
וההבדל אינו ה-`sameAs` שחסר בשתיהן** אלא שם ותפקיד מאומתים שאפשר לתלות בהם
`knowsAbout`.

שבע בדיקות רגרסיה ב-`test_version_guard.py`. **הכלל נפל על עצמו בריצה
הראשונה** ותפס את רשומת ה-changelog שמתעדת אותו, ולכן אזכור בתוך backticks
נחשב תיעוד ולא שימוש. אותו טיפול שכבר קיים בכלל `באוכנט`.

---

> **2026-08-16 — D2 שלב ב: סמכות ארגונית לפלרום**

**`pal_lint.py` v1.12.0 → v1.13.0**

שלוש השכבות סתרו: `SITES` הגדיר `expert="דניאל"` ואכף אותו, `project-plrom.md`
החזיק "עוגן סמכות: מומחה שירות פלרום" בלי שם, והזיכרון סימן "טרם אומת".
בפועל כל מאמר פלרום ייצר ישות `Person` בלי `jobTitle` ובלי `sameAs`.
**ישות אדם שאי אפשר לאמת אי אפשר לצטט**, וציטוט הוא המדד שאנחנו מנסים להזיז.

**הכרעת גיל: עוגן ארגוני, לא אדם.** `plrom.expert = None`, `"דניאל"` עבר
ל-`wrong_experts`, ושדה חדש `author_org_only` (plrom=True). שני כללים חדשים:
`PERSON_ENTITY_FORBIDDEN` (ERROR) על ישות `Person` ב-@graph, ו-`AUTHOR_NOT_ORG`
(ERROR) על `author` שמצביע על `#content-author`. הודעת `EXPERT` מטפלת
ב-`expert=None`.

**`plrom_person_bad.html` — fixture חדש** עם ישות Person, author שגוי ושם
פרטי ב-author-bio. selftest מצפה לשלושת הכללים.

**קבצי סקיל:** `project-plrom.md` סעיף "מומחה" הוחלף ב"סמכות (E-E-A-T
ארגוני)" עם הוראת ההיפוך; `html-template.md` v7.13 מסמן את החריג בשלד
ה-@graph; `SKILL.md` v8.15.

**הפיך בדגל אחד.** שם מלא + תפקיד + `sameAs` חיצוני אחד מחזירים את פלרום
ל-6 ישויות. אין למלא חלקית.

---

> **2026-08-16 — D2 שלב א: שכבת הקישורים של מרום**

**`pal_lint.py` v1.11.1 → v1.12.0**

**`MAROM_PC_LINK` נמחק (היה ERROR).** הכלל נכנס ב-v1.2.3 עם הנימוק שכתוב
בקוד: "קישור product-category = כמעט תמיד 301/404". הצלבה מול
`cats/marom_page_queries.json`: **420 עמודי `/product-category/`, 308,604
חשיפות, 12,847 קליקים, 209 עם קליקים בפועל, 73 במיקום 1-3.**
`/product-category/sharp/` לבדו 1,520 קליקים במיקום 1.0. המוסכמה שהכלל דרש
במקום, `/[brand]-אביזרים-וחלפים/`, מחזיקה 9 עמודים ו-10,967 חשיפות.
הכלל חסם פי 28 ממה שהגן עליו. חמור מזה: `preflight` המליץ על
`/product-category/sharp/מקרר-4-דלתות/` (1,735 חשיפות, מיקום 1.3) כנכס
ששולט, ו-`postflight` חסם אותו. זו חזרה מדויקת של כשל v8.12, שבו כלל אחד
המליץ ואחר חסם. הבעיה שהכלל נועד לפתור, כתובות מומצאות, מכוסה במלואה
ב-`link_audit` שנכנס באותה גרסה עצמה ומחייב `check_url` 200 חי לכל קישור
פנימי. כלל גס ששרד אחרי שהפתרון הנכון נכנס.

**`BRAND_LINK_UNKNOWN` (ERROR, מרום) — כלל חדש.** קישור ל-`/brands/` שאינו
ברשימה הסגורה של 17 עמודי המותג המאושרים נחסם. תופס שגיאת כתיב בכתובת
מותג, למשל `/brands/kitcheaid-service/`, שעברה בשקט עד היום. **הכרעת גיל:
רשימה סגורה ולא תבנית**, כי אין כוונה להוסיף עמודי שותפים חדשים, ורשימה
מפורשת ניתנת לביקורת. הרשימה כוללת גם את `/kitchenaid-parts/` ואת
`/magimix-parts/`, שהם נכסים שלנו ולא עמודי שותף.

**`base_brand_path()` — נרמול קידומת שפה.** הכרעת גיל: עמוד מתורגם הוא אותו
עמוד. `/en/brands/amana-service`, `/ru/brands/moulinex-service` ו-`/en/brands/magimix-service`
קיימים בפועל ב-GSC. הנרמול מסיר `en|ru|ar|fr`, דומיין, פרמטרים וסלאש סוגר.

**רשימת האסורים לא השתנתה.** 16 נתיבים, זהים לחלוטין למה שנאכף קודם.
אפס רגרסיה בהגנה.

**fixtures ו-selftest:** `marom_bad` החליף את קישור ה-product-category
בקישור `/brands/` בכתיב שגוי, ומצפה ל-`BRAND_LINK_UNKNOWN` במקום
`MAROM_PC_LINK` (19 כללים). `marom_good` קיבל שלושה קישורים שחייבים לעבור:
`/product-category/sharp/מקרר-4-דלתות/`, `/kitchenaid-parts/`
ו-`/en/brands/amana-service/`.

**`project-marom.md`:** סעיף הקישורים נכתב מחדש. הסתירה הפנימית הוסרה,
הקובץ אמר בשורה 43 "לכל מותג" ובשורה 48 רשימה של שמונה.

---

> **2026-08-13 (ערב) — סגירת כפילות D1**

**`version_guard.py` v1.1 → v1.2 — `CHANGELOG_DUPLICATE` ו-`CHANGELOG_DRIFT` (ERROR)**
הבדיקה הקודמת ירתה רק כשמספר גרסה הופיע ב**יותר** משני מקומות, כלומר היא
התירה במפורש שכל רשומה תחיה גם ב-`SKILL.md` וגם ב-`CHANGELOG.md`. 21 רשומות
עשו בדיוק את זה, ו**ארבע מהן התפצלו לשני טקסטים שונים תחת אותו מספר גרסה**:
v7.18 (44.8% דמיון), v7.19 (37.1%), v7.20 (19.1%), v8.10 (40.5%). זה הכשל
המדויק שהכלי נבנה למנוע ב-2026-08-10, והוא אישר אותו בירוק שלושה חודשים.
מעכשיו: הבעלים של ההיסטוריה הוא `CHANGELOG.md`, `SKILL.md` מחזיק את הגרסה
הנוכחית בלבד, וכשהיא מופיעה בשניהם הטקסט חייב להיות זהה תו בתו.
`VERSION_DUPLICATE` שונה לכפילות בתוך אותו קובץ.

**`skills/content-machine/SKILL.md` — 20 רשומות היסטוריה נמחקו (69.7KB → 54.8KB, -21.4%)**
לפני המחיקה הוצלבה כל אחת מ-21 הרשומות מול הקוד ומול גוף הקובץ: 16 מזכירות
קודי כללים שכולם ממומשים ב-`pal_lint`/`postflight`/`preflight`, וחמש הנותרות
(v8.12, v8.10, v8.6, v7.20, v7.18) מוחזקות בגוף `SKILL.md` עצמו (שורות 95,
162, 316, 412-417, 475) או נאכפות בקוד (`postflight` 150-153, 519).
**אפס תוכן ייחודי אבד.** שלוש מארבע הרשומות שהתפצלו היו דווקא בחמישייה
שאין לה קוד מאחוריה, וזה לא מקרה: רשומה שמגובה בכלל מתוקנת כשהכלל משתנה.

**`skills/content-machine/CHANGELOG.md` — תיקון עובדתי ב-v8.10**
הרשומה נשאה `brief_partial` בגודל 17KB. הרצת `preflight --phase plan --site marom`
על הדאטה החי מחזירה **42KB** (ו-`brief_full` 123KB), כלומר -79% ולא -91%.
הגרסה שהייתה ב-`SKILL.md` החזיקה את המספר הנכון. תוקן, ותו U+2011 שהיה
בשורה הוחלף במקף רגיל.

**`tools/tests/test_version_guard.py` — קובץ בדיקות חדש, שער CI שביעי**
14 בדיקות ב-sandbox: מסלול תקין ירוק, רשומה ישנה בשני הקבצים נחסמת, 13
רשומות משוכפלות נחסמות, דריפט טקסט תחת אותה גרסה נחסם, טקסט זהה מותר,
כפילות בתוך אותו קובץ נחסמת, ו-`VERSION_REGRESSION`/`VERSION_UNLOGGED`
לא נשברו. הרקע: `version_guard` אכף כללים על כל הריפו ומעולם לא נבדק בעצמו.
סדר השערים: ledger_lint → selftest → test_flight → test_postflight →
test_ledger_tools → **test_version_guard** → version_guard.

---

> **2026-08-13**

**postflight.py v1.4 → v1.5 — כלל חדש `DOMINANT_H1_DUPLICATE` (ERROR, חוסם)**
רץ מיד אחרי `check_dominant_links`. לוקח מ-`brief["dominant_pages"]` רק נכסים
שלנו (לא partner) במיקום 3.0 ומטה, ומצליב את מילות התוכן של השאילתה מול ה-H1.
כיסוי מלא של כל מילות השאילתה ב-H1 = חסימה. חריג יחיד: Refresh של אותו עמוד
דומיננטי עצמו.
הרקע: שני מאמרי שארפ במרום נכתבו על "חלקי חילוף למקרר שארפ", אשכול שעמודי
ה-product-category שלנו מחזיקים במיקום 1.0-1.5 עם 19,687 חשיפות. שני המאמרים
על אפס חשיפות. `dominant_pages` כבר היה ב-brief באותו רגע, אבל רק המליץ לקשר
(WARN), ולכן אף שער לא עצר את הכתיבה. המלצה אינה שער.

**ledger_lint.py — כלל חדש `STALE_NOT_YET` (WARN בלבד)**
תא שאילתות שמסומן `טרם צבר (YYYY-MM)` נבדק מול תאריך הפרסום בעמודה הראשונה,
ובנפילה חזרה מול החודש שבסוגריים. מעל 90 יום נרשמת אזהרה עם מספר הימים וה-URL.
הרקע: 8 שורות סומנו "טרם צבר" בסבב הסיווג של 2026-08-13, בלי שום מנגנון
שמזכיר לבדוק אותן שוב. "טרם צבר" הוא מצב זמני, ואחרי רבעון בלי חשיפות זה ממצא.
WARN ולא ERROR, כדי שלא לחסום את ה-CI על חלוף זמן בלבד.

**tools/tests/test_ledger_tools.py — קובץ בדיקות חדש, שער CI שישי**
sandbox ב-tempfile שמעתיק את `ledger_patch.py` ו-`ledger_lint.py` ומריץ אותם
ב-subprocess על לדג'ר מינימלי. מכסה: החלת date ו-queries, שורה שכנה שלא נגעה,
ריקון התור, ריצה חוזרת על תור ריק, URL שלא נמצא (exit 1 והלדג'ר לא נגע),
התאמה על URL עם percent-encoding, ו-STALE_NOT_YET על 30 יום מול 200 יום.
הרקע: `ledger_patch` כותב ישירות למקור האמת של שער ה-dedup, ועד היום נבדק רק
בהרצה אמיתית על הקובץ האמיתי. באג בהתאמת URL היה משכתב שורה שגויה בשקט.

**.github/workflows/pal-state-lint.yml — שער שישי**
`Ledger tools tests` נוסף לפני `Version guard`. סדר השערים:
ledger_lint → selftest → test_flight → test_postflight → test_ledger_tools →
version_guard.
