# DECISIONS — יומן החלטות מתגלגל
מה הוחלט ולמה. לא רק מה. החדש למעלה.

---

## 2026-06-29 — הקמת שכבת pal-state
**מה:** ריפו נפרד `pal-state` כשכבת מצב חי, מנותק מהזיכרון המובנה.
**למה:** הזיכרון המובנה הוא תמונת מצב סטטית שמתעדכנת ברקע ובאיחור, לא ניתן לבקרה או צפייה. מצב חי (סטטוס, גרסאות, פתוחים) חייב שכבה גלויה ומבוקרת-גרסה. עירוב כללי ברזל לתוך הריפו היה שובר את עיקרון מקור-אמת-יחיד, לכן הופרד חד: ברזל בזיכרון, מצב בריפו.
**השלכה:** טריגר טעינה חייב להיכנס לזיכרון המובנה כדי שכל instance יקרא את הריפו בתחילת שיחה.

---

## רקע — החלטות ליבה קודמות (תקציר, המקור בזיכרון)
- content-machine v7.7: CSS hex קשיח בלבד — אחרי שזוהה ש-var()/tokens חוסמים publish בבלוגים.
- brand-hub-machine v1.12: offers block חובה ב-Product schema — פותר שגיאת GSC על offers/review/aggregateRating חסר.
- backslash מ-geresh characters (קיצ'נאייד) = שורש חוסם save בעמודי brand-hub. תוקן.
- NOTES.md לא persists בין שיחות → זיכרון מובנה הוא שכבת persistence יחידה.
- Bing AvgImpressionPosition: מיקום שלם אמיתי. ה"quirk" הישן של חלוקה ב-10 היה שגוי, אומת חי יוני 2026.


## 2026-07-03 — ארכיטקטורת בקרת איכות: שתי שכבות, לא צוות סוכנים
- הוחלט: לא בונים ריבוי סוכני LLM לבדיקת פלטים. שכבה 1 = pal-lint (Python דטרמיניסטי,
  כל כללי הברזל, gate חוסם, exit code). שכבה 2 = reviewer יחיד בקונטקסט נקי רק לשיפוט
  (dedup, טבעיות עברית, אימות מול MCP).
- נימוק: רוב הטעויות ההיסטוריות הן הפרות כללים דטרמיניסטיים. סקריפט תופס אותן ב-100%
  אמינות ואפס עלות. סוכן LLM לבדיקות regex = פחות אמין ויותר יקר.
- אכיפה: כלל ברזל בזיכרון — אין הגשת HTML בלי דוח pal-lint ירוק. selftest חובה אחרי כל
  עדכון כללים.
