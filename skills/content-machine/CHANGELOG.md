# content-machine — CHANGELOG

היסטוריית גרסאות מלאה. הגרסה הנוכחית ושורת השינוי שלה בראש SKILL.md.

> **v8.8 (סיווג כוונת עמוד, 2026-08-10):** חיבור GA4 חשף שעמודי מוצר ממירים 30-37% ומאמרי תוכן אפס, ומכאן עלתה מסקנה חפוזה ש"כל מאמר חייב להמיר". **גיל תיקן: לא כל עמוד חייב להמיר.** (1) `preflight` מסווג כל נושא ל-`conversion`/`authority`/`service`, וכל אחד נמדד אחרת. (2) `NO_CONVERSION_PATH` (ERROR) חל **רק** על conversion — עמוד service שפתר בעיה וחסך שיחה למוקד עשה את עבודתו. (3) מחיר מרום נוסף: 290 ₪ לכל המוצרים, למעט וייקינג ופיאבה. **הסדר בעידן AI Overview: להיות מצוטט ← להביא תנועה ← לתת ערך ← לאפשר פעולה**, והפער הגדול הוא בשלב הראשון (0/4 בשאילתות טכניות).

> **v8.7 (כלל ההמרה, 2026-08-10):** ריצת האימות הראשונה הפיקה מאמר שעבר 99 כללים ועדיין לא היה ממיר: על "כמה עולה ביקור טכנאי" הוא ענה "העלות תלויה, פתחו קריאת שירות". זו הפניה ולא תשובה, וזה בדיוק מה שמייצר CTR של 0.25% על עמוד עם 73,539 חשיפות. (1) **`EVASIVE_ANSWER` (ERROR)** — שאלה מסחרית בלי מספר בתשובה נחסמת. (2) **`brief.pricing`** — מחירים מאומתים מוזרמים לכל ריצה (CSB 349 ₪; פלרום 390/340/290 ₪; מרום ממתין ואין לנחש). (3) כלל ההמרה נכנס לסקיל כהנחיה מרכזית: המאמרים נועדו להמיר, לא רק לדרג.

> **v8.6 (סגירת החוליה הידנית האחרונה, 2026-08-09):** שורת ה-ledger הועתקה ידנית עד היום, ונשכחה חמש פעמים — ובכל פעם שער הדדופ נחלש. `postflight` כותב עכשיו ל-`ledger-pending.md`, ה-workflow `ledger-merge` ממזג ל-`content-ledger.md`, ו-`[PENDING:slug]` מוחלף ב-URL אמיתי מ-`gsc_page_queries` (כל שני). **עקרון מנחה: פעולה ידנית חוזרת היא פגם בעיצוב, לא תהליך.** ההעלאה לאלמנטור נשארת הפעולה האנושית היחידה.

> **v8.5 (Mode Refresh בר-ביצוע, 2026-08-09):** המכונה ידעה להחליט "רענן" ואז חסמה את עצמה — `postflight` בדק H1 רק מול `allowed_topics`, ולכן כל מאמר Refresh נפל ב-`TOPIC_UNAUTHORIZED`. (1) `matched_topic` מזהה גם `refresh_queue`. (2) **תור ה-Refresh עבר לרמת עמוד ולא שאילתה** — 51 שורות כפולות ל-CSB הפכו ל-7 עמודים ייחודיים, ממוינים לפי חשיפות. (3) כל עמוד מגיע עם `ranking_for` (מה לא לשבור), `gap_queries` (מיקום 11-40 — שם מוסיפים), ו-`frozen`. (4) שלושה כללים חדשים: `REFRESH_DATE`, `REFRESH_URL_CHANGED`, `REFRESH_NO_GAIN`. (5) **שלב 2ב חדש בסקיל.**

> **v8.4 (סגירת שני פערי אכיפה, 2026-08-06):** (1) **`competitor_headings` הפך לקריאה נדרשת בשלב 0** — `INFO_GAIN_DIFF` ביקש את השדה ואף שלב לא ייצר אותו, ולכן בדיקת ה-Information Gain התריעה "אין מיפוי" בכל ריצה בלי לפעול. המקור: `web_search` על שאילתת היעד + חילוץ H2 מ-3-5 התוצאות המובילות. (2) **`preflight` כבר לא נופל בשקט בלי MCP** — הודעה מפורטת מה חסר ולמה, ו-`--allow-degraded` לטיוטה מסומנת. `postflight` חוסם אותה ב-`DRAFT_BRIEF` (ERROR), כדי שטיוטה לא תתפרסם בטעות.

> **v8.3 (Entity Disambiguation, 2026-08-06):** ישות `Brand` מקבלת `sameAs` לוויקידאטה ולוויקיפדיה מרישום מאומת (`pal-state/brand-entities.md`, 11 מותגים אומתו, השאר ממתינים). זה מקשר את המאמר לגרף הידע העולמי ומסיר ערפול. האימות חשף שלוש מלכודות: `Q48757454` הוא "Sharp — יצרן רובי אוויר"; לבוש ולסימנס כמותגי מוצרי חשמל אין ישות נפרדת והנכון הוא BSH; Constructa מופיעה כשני ערכים. `BRAND_SAMEAS_MISSING` (WARN) ו-`BRAND_SAMEAS_WRONG` (ERROR, קישור לתאגיד האם). pal-lint → v1.10.0, html-template → v7.10.

> **v8.2 (קטלוג וידאו, 2026-08-06):** שלב 7 הפך לדטרמיניסטי. `youtube_pull.py` מושך את 126 הסרטונים משלושת הערוצים (properties של ערוצים אינם מוחזרים מ-Search Console API, לכן YouTube Data API), ו-`preflight` מחזיר ב-brief מזהה מאומת או `null`. ההתאמה דורשת פעולה משותפת + סוג מוצר משותף, אחרי שהגרסה הגסה הדביקה סרטון התקנה למאמר על קוד תקלה E15. `VIDEO_MISSING` ב-postflight מתריע כשקיים סרטון ולא הוטמע. בנוסף: `BRAND_HUB_MISSING` (ERROR) — כל מאמר חייב קישור ל-/brands/, כי עמודי השותפים קולטים 264K חשיפות ועמודי המותג שלנו על אפס.

> **v8.1 (השלמת מיכון, 2026-08-03):** סריקת שלמות על 90 האמירות המחייבות בחמשת קבצי הסקיל. ארבעת פריטי השיפוט של v8.0 מוכנו: `CONTRAST_RATIO`, `NARRATIVE_MISSING`, `AUDIENCE_ANCHOR`, `INFO_GAIN_DIFF`. חמישה פערים שהסריקה חשפה נסגרו ב-pal-lint v1.8.0: `SUPERLATIVE`, `FOCUS_OUTLINE`, `PREF_STRIP_ORDER`, `PERMALINK_CASE`, `TERM_PLROM_NAME`. **93 כללים נאכפים, שתי הכרעות עורכיות נותרו.**

> **v8.0 (Preflight/Postflight, 2026-08-03):** הסקיל הפסיק להיות רשימת הוראות והפך לנהג של שני סקריפטים. **שלב 0 = `preflight.py`** — הסשן לא בוחר נושא אלא מקבל `allowed_topics`, לא ממציא מונח אלא שולף מ-`vocabulary` מאומת, ולא מנחש קישור אלא מקבל מאומתים. אנטי-קניבליזציה מוכרעת לפי מיקום GSC (4-20 = Refresh) ולא לפי אחוז חפיפה, בעקבות מחקר שמראה שחפיפת שאילתות אינה אינדיקטור. **שלב 13 = `postflight.py`** — 77 כללי pal-lint + 7 בדיקות מול ה-brief, כולל `TERM_VERIFY` שהיה תופס את "מגנטרון" ו"מיקרוגל נייד". **צ'קליסט השיפוט ירד מ-30 פריטים ל-4.**

> **v7.23 (יישור קו, שלב A, 2026-08-03):** מיזוג שתי גרסאות v7.22 שנוצרו במקביל בשני סשנים ונתנו לתוצאות שונות אותו מספר. נכנס הכל: (1) **`H1_LISTICLE`** — דפוס ליסטיקל ב-H1 נדחה שלוש פעמים, עכשיו WARN בלינט. (2) **אימות מונחי מוצר** — כל מתאר סוג מוצר חייב מקור מאומת; טבלת הזוגות שנתפסו. (3) כתיב מותגים (באוקנכט, קיטשן אייד). (4) **בלוק Organization קנוני לשלושת האתרים** — הפער ש-`ORG_SCHEMA_DRIFT` אכף בלי מקור נסגר. (5) `project-csb.md`: תוקן "אין ערוץ ייעודי" ← `@csbinc` (סומן שלוש פעמים מאז 9.7). (6) מקור וידאו דרגה 1 = embed בעמוד המוצר. pal-lint → v1.7.0.

> **v7.22 (כתיב מותגים ו-Organization קנוני, 2026-08-03):** (1) **"באוכנט" ← "באוקנכט"** — הכתיב הישן לא קיים בשוק ומקורו בעמוד שירות ישן שלנו; ניתוח GSC הראה ש-19,432 מהחשיפות שנראו כביקוש היו ה-meta description שלנו. ERROR ב-pal-lint. (2) **KitchenAid: "קיטשן אייד" ראשי** (51,504 חשיפות, 2,073 קליקים) ו"קיצ'נאייד" משני (10,571, 411); שמות מוצרים מ-MCP נשארים verbatim. (3) **בלוק Organization קנוני נוסף לקובץ הפרויקט** — `ORG_SCHEMA_DRIFT` מ-v1.6.0 אכף `sameAs` שאף קובץ לא הגדיר. הפער נסגר.

> **v7.21 (זווית בידול ומקורות מתחרים, 2026-08-03):** (1) **שלב 4:** קריאת עמוד המותג לפני מחקר חיצוני — חובה. מאמר "מצב שבת שארפ" פספס את "מהדרין" ואת אישור המכון הטכנולוגי להלכה, שניהם קיימים בעמוד המותג שלנו. (2) **`COMPETITOR_SOURCE` (ERROR):** מידרג והמקצוענים אסורים כמקור מצוטט או מקושר. (3) **`ORG_SCHEMA_DRIFT` (ERROR):** Organization נלקח verbatim מקובץ הפרויקט. (4) **`KEYWORD_DENSITY`:** היעד 1-2% נמדד על ראש הביטוי, לא על ביטוי בן 5 מילים שלעולם לא יגיע לשם בעברית טבעית. pal-lint → v1.6.0.

> **v7.20 (סגירת ליקויי ריצת "וו גיטרה" מרום, 2026-08-03):** אודיט של פלט בלוג חי (מרום, "וו גיטרה או וו לישה") אימת מול האתר שהסכמה, כרטיס ה-Hero, המחירים והקישורים תקינים, אבל חשף שלושה ליקויים שאף כלל קיים לא תפס. כל שלושתם נסגרו בשורש עם אכיפה מכנית ב-pal-lint v1.5.0, לא בהנחיה בלבד. (1) **`SOURCES_INTERNAL_TOOL` (ERROR):** רשימת המקורות הגלויה כללה "נתוני Search Console ו-Bing Webmaster Tools" — כלי בחירת נושא שאינם מקור לשום טענה, וחושפים מתודולוגיה פנימית ללקוח. שלב 8 מגדיר עכשיו רשימת מקורות סגורה: יצרן, תקן, מחקר מצוטט, ניסיון מעבדה. (2) **`DANGLING_CONNECTOR` + `TRANSITION_STACK` (WARN):** המשפט "וו לישה, אך הוא מושך ומקפל" יצא שבור תחבירית, ו-71% מהפסקאות נפתחו במילת קישור בזמן ש-Yoast הראה ירוק. שלב 9 קיבל שלוש מגבלות מפורשות. הכלל מחריג משעבדים טבעיים (כדי, מכיוון ש, אחרי ש) כדי למנוע רעש. (3) **`PHONE_CANON` (WARN):** `2620*` תקף אך אינו קנוני; `*2620` הוא הצורה לתוכן חדש. (4) **שלב 7, מקור וידאו דרגה 1:** עמוד המוצר של ה-Hero נבדק ל-`youtube.com/embed/` לפני פנייה לערוצי האתר והמותג. בריצה שנבדקה היה סרטון רשמי מוטמע בעמוד ה-Hero והמאמר ויתר על בלוק הווידאו. עלות הבדיקה אפס, כי העמוד נמשך ממילא.

> **v7.19 (תיקון דריפט חוצה-קבצים, 2026-08-03):** אודיט v7.18 בדק את SKILL.md בלבד והחמיץ סתירה ב-`html-template.md`: הכותרת אמרה "6 entities, או 7 עם כרטיס מוצר" והשלד כלל בלוק `Product` מלא, בניגוד ל-SKILL v7.15+ (6 ישויות תמיד) ול-`SCHEMA_PRODUCT_BLOG` שנאכף כ-ERROR ב-pal-lint. הדריפט שרד שלוש גרסאות. (1) בלוק `Product` נמחק, `mentions` = Brand בלבד, `#content-product` הוסר; השלד אומת: 6 ישויות, JSON תקין. `html-template.md` הועלה ל-v7.9. (2) נוסף פרוטוקול bump לסעיף "קבצי עזר": כל שינוי כלל מחייב הצלבה מול html-template, master-prompt, project-*, common-core ו-pal_lint.py לפני שחרור.

> **v7.18 (גל 1 אופטימיזציה — עלות ריצה וזרימה רציפה, 2026-08-03):** אודיט מלא של SKILL.md זיהה שרוב הנפח הוא הוראות שנכתבו לפני שהתשתית התומכת נבנתה. (1) **שלב 13 מ-6,230 ל-3,316 תווים** — 35 צ'קבוקסים שנאכפים אוטומטית ב-pal-lint v1.4.0 נמחקו; נשארו ~20 פריטי שיפוט בלבד + טבלת מדיניות WARN per-rule שמגדירה מה מתקנים לבד ומה באמת חוסם. (2) **שער dedup: `recent_chats` מ-20-30 ל-`n=10`** — הכלל נכתב לפני שקיים content-ledger.md; מאז v7.16 ה-ledger הוא המקור הדטרמיניסטי וה-recent_chats גיבוי בלבד. חיסכון מוערך 20-40K טוקנים לריצה. (3) **`check_url` (WooCommerce MCP) מחליף `web_fetch`** ב-link_audit (שלב 13) ובחילוץ canonical ל-cluster (שלב 6) — סטטוס+redirect+canonical בלבד, עוקף WAF, בלי משיכת עמוד מלא. `web_fetch` נשאר גיבוי. (4) **חוזה אי-הפרעה** בסעיף "מצב אוטונומי" — שלוש עצירות מותרות בלבד (כשל clone/לינט חסר, החלטה עסקית ללא מקור, בקשה מפורשת). (5) **Mode Refresh = עץ החלטה דטרמיניסטי** לפי זהות כוונה, בלי לשאול. (6) דיווח בחירת נושא הוגדר חד-כיווני. (7) `html-template.md` הוסר מ"קרא תמיד" (סתר את טבלת קבצי העזר) — נטען בשלב 11 בלבד, ~27KB מהרצפה. (8) v7.16 הועבר מ-SKILL.md לכאן.

> **v7.17 (תיקון @id + TOC, 2026-07-23):** (1) **SCHEMA_ID_YOAST_COLLISION נאכף בפועל** — pal-lint v1.4.0 עם הכלל (ERROR) שמונע @id שמסתיים ב-#article/#organization/#breadcrumb/#website/#primaryimage (מתנגש עם ה-@graph האוטומטי של Yoast). מוסכמה: content-machine→`#content-*`. (2) **html-template.md → v7.8:** תוקן WCAG_HEADING_SKIP — ה-TOC עבר מ-`<h3>` ל-`<p class="toc-title">`.

> **v7.16 (יישום אודיט הסקילים, 2026-07-06):** (1) **common-core.md** — הכללים המשותפים לשלושת הסקילים אוחדו לקובץ אחד; הסקיל מפנה, לא משכפל. (2) **מקור דאטה שלישי:** `autocomplete_customer_language.md` בשלב 0 ובשלב 4. (3) **Mode Maintain:** תור רענון יזום מ-`ledger-performance.md`. (4) **שני פורמטים חדשים בשלב 2:** אינדקס קודי תקלה per-מותג, ומדריך עלות עם טווחי מחיר מ-MCP בלבד. (5) היסטוריית הגרסאות עברה לקובץ זה. pal-lint v1.3.0 אוכף anchor/link-budget/speakable.

> **v7.15 (pal-lint v1.2 כמקור יחיד + ביטול ישות Product בבלוג, 2026-07-05, audit חוצה-סקילים):** (1) **כל הבדיקות המוטמעות נמחקו משלב 13** — CONTENT, yoast_check, link_audit ו-schema_deep נקלטו ב-pal-lint v1.2.0 (pal-state). שלב 13 = הרצת pal-lint בלבד (עם `--keyword`), ואז web_fetch לקישורים שהלינט מדפיס. אפס לוגיקת בדיקה בסקיל, אפס דריפט. (2) **ישות Product בוטלה ב-@graph של בלוגים** — גוגל דורש offers/review/aggregateRating בכל Product, וכלל ה"בלי offers" הישן ייצר את שגיאת ה-GSC ש-brand-hub v1.12 תיקן. הפתרון: דף המוצר מחזיק את ה-Product/offer החי; בבלוג הכרטיס הוויזואלי נשאר, mentions מצביע על Brand בלבד, @graph = 6 ישויות תמיד. pal-lint אוכף (SCHEMA_PRODUCT_BLOG). (3) מילון ה-transitions עבר ללינט (מקור יחיד, גבולות מילה; "כמו כן" הוצאה).

> **v7.14 (שחזור לקחי v7.10-v7.11 שאבדו במעבר, 2026-07-05):** v7.12 נבנתה על v7.9 המותקנת, ושלושה לקחים מ-v7.10-v7.11 (שנערכו בסשן 2.7.2026 ולא שרדו כ-ZIP) שוחזרו מהיסטוריית השיחות: (1) site:search בשער הדדופ דורש **2+ ניסוחים שונים** לפני קביעה שנושא פנוי (שלב 2.ג). (2) **slug חתוך נשאר חתוך verbatim** גם כשנראה שגוי — "תיקון" גרם 404 (שלב 0). (3) **כל קישור בקובץ מיזוג/עדכון מגיל עובר אימות web_fetch מחדש**, כולל ותיקים (צ'קליסט). recent_chats-חובה מ-v7.10 כבר היה מכוסה. הבסיס התקני מעתה: השרשרת המלאה v7.9→v7.12→v7.13→v7.14, אין גרסאות אבודות.

> **v7.13 (מקור יחיד לבדיקות Elementor, 2026-07-05):** pal-lint v1.1.0 נדחף ל-pal-state עם כיסוי מלא של כל חוסמי ה-Elementor (כולל 9 שחסרו ב-v1.0.0). בלוק ה-VALIDATE Elementor המוטמע נמחק משלב 13 — הבדיקה היא הרצת `tools/pal_lint.py` מה-clone של pal-state בלבד. בדיקות התוכן (CONTENT/YOAST/LINKS/DEEP) נשארות מוטמעות עד קליטתן ב-pal-lint v1.2.

> **v7.12 (סגירת 13 פערי audit, 2026-07-05):** (1) **מרום: get_categories אסור** — נעילה גלובלית של שרת ה-MCP; search_products בלבד (שלב 0, שלב 3). (2) **כלל decode ל-permalink** — החריג היחיד ל-verbatim: פענוח percent-encoding לעברית raw בווידג'ט Elementor, decode בלבד, לעולם לא encode (שלב 0). מיישב את הסתירה מול בדיקת ה-%XX ב-VALIDATE. (3) **שער אנטי-קניבליזציה** בשלב 2: מיקום 4-25 = עמוד קיים כבר מדורג; אם זה מאמר בלוג — ברירת המחדל רענון (Mode Refresh), לא מאמר חדש מתחרה. (4) **content-ledger.md** (pal-state) = מקור dedup ראשון ודטרמיניסטי; שלושת המקורות הישנים הפכו לגיבוי. שלב 14 מפיק טיוטת שורת ledger. (5) **עמוד המותג (brand hub)** נכנס לתקציב הקישורים (שלב 3) ונבדק ראשון כמקור קישור נכנס (שלב 6); הרשימה per-site בזיכרון המובנה. (6) **עונתיות** כסיגנל משני בבחירת נושא. (7) **שלב 13 הורחב**: pal_lint.py (pal-state) הוכרז מקור האמת לבדיקות Elementor, ונוספו שלושה בלוקים אוטומטיים — yoast_check (משפטים/transitions/אורך/H1), link_audit (HTTP חי על כל href פנימי), schema_deep + WCAG + קישורים אסורים per-site. הלקח של v7.9 הוכלל: כל מדד מספרי נאכף בקוד, לא בספירה ידנית. בסיס הגרסה: v7.9 המותקנת.

> **v7.9 (אכיפת Zero Hallucination + איכות — לקח ממאמר טפאל):** audit של בלוג שפורסם חשף שכלל איסור הסטטיסטיקות המומצאות קיים אך לא נאכף (מאמר עבר עם "כ-70% מהמקרים, לדברי הצוות"). התיקון הוא אכיפה, לא ניסוח מחדש. (1) **שער VALIDATE תוכן חדש** (שלב 13, בלוק שני): תופס אחוז/מספר בטקסט (ודא מקור מאומת), מק"ט עם מקף בקצה (חתוך/שגוי), ו-H2 עם id שחסר ב-TOC. (2) **תקרת transitions** ב-master-prompt: 30% עד ~45%, לא over-use (המאמר הגיע ל-63%, נקרא מכני). (3) דוגמת ההזיה ב-master-prompt חוזקה עם הניסוח שנכשל בפועל ("לדברי הצוות, N%"), והובהר שייחוס לניסיון שטח אינו הופך מספר למאומת. brand-hub-machine קיבל את שער סטטיסטיקה המקביל (v1.14).

> **v7.8 (E-E-A-T מבוסס הוכחה + עיגון קהל — Campixx 2026):** ארבעה שיפורים מדוח הפערים מול תובנות הכנס. (1) **author-bio ו-Person schema מבוססי הוכחה** במקום ביו גנרי (Tom Winter: "Trust זה לא ביוגרפיית כותב"). מקור אמת חדש: סעיף "מומחה" בקובץ הפרויקט (עוגן סמכות מאומת + התמחות + הוכחת ניסיון). שדות אישיים לא מאומתים = placeholder שגיל ממלא, לעולם לא המצאה. (2) **עיגון קהל מפורש** ב-Direct Answer (Dan Shure): מי הלקוח והכאב שלו נכתבים במפורש, לא מרומזים. הכלל ב-master-prompt (סעיף חדש) ובצ'קליסט. (3) **נרטיב מותג קנוני** — סעיף "נרטיב מותג" חדש בקובץ הפרויקט (5 משפטים קנוניים + תכונות), מוזרק ל-Direct Answer ול-Organization schema לעקביות חוצת-עמודים (Lazarina Stoy). (4) **FAQ מבוסס 4 ממדים + Query fan-out** (Dan Hinckley, Alex Galinos) ב-master-prompt. brand-hub-machine קיבל את אותם ארבעה שיפורים (v1.13).

> **v7.7 (תיקון Elementor קריטי — צבעי hex קשיחים):** בלוק ה-tokens `--bh-*` + `var()` שהוכנס ב-v7.4 הוכח כגורם החד-משמעי לכשל פרסום בלוגים ב-Elementor ("משהו לא תקין"/"החיבור אבד"). ניסוי צלב (יוני 2026): אותו עמוד עם בלוק tokens נכשל בשמירה, אותו עמוד עם **hex קשיח** נשמר ופורסם תקין. התבנית הומרה לחלוטין ל-hex קשיח (אפס `var()`, אפס בלוק tokens, אפס הערות CSS), עם **טבלת צבעים לפי אתר** ב-html-template להחלפה מודעת-תפקיד (CSB/Marom/Plrom). שער VALIDATE (שלב 13) תופס עכשיו `var(--bh-*)`, בלוק tokens, הערות CSS, ו-backslash בתוכן גלוי. הצבעים הזהותיים נשמרו (CSB כתום, Marom navy+אדום, Plrom כתום) — רק המנגנון השתנה.

> **v7.6 (Bing חי כמקור חיפוש משלים):** נוסף מקור דאטה שני לחיפוש לצד GSC. שלב 0 קורא גם את `bing_customer_language.md` (מגיע עם אותו clone של pal-gsc, לצד `gsc_customer_language.md`). שלב 4 מפיק "יהלומי Bing": שאילתות עם חשיפות ב-Bing שאינן ב-GSC, כמקור ל-H2/FAQ ייחודיים שמייצרים Information Gain. המקור מתחדש ע"י `bing_pull.py` שב-repo. **מיקומי Bing הם מיקום אמיתי בשלמים (1=ראשון), לא מוכפלים ב-10.**

> **v7.5 (איחוד מקורות אמת):** הוסרו כפילויות. כל כלל יושב במקום מוסמך אחד, השאר מפנים אליו. שלב 9 מפנה ל-`master-prompt.md` לאופן הכתיבה (טון, מונחים, שפת לקוח) במקום לשכפל. שלב 11 מפנה ל-`html-template.md` ל-CSS וכללי נגישות. כלל ה-permalink מעוגן בשלב 0. `youtube-channels-reference.md` נמחק (היה כפילות מלאה של קבצי הפרויקט) — מקור האמת לערוצים הוא `project-*.md`. **טבלת "הזדמנויות תוכן GSC" הסטטית הוסרה משלושת קבצי הפרויקט** — הייתה כפילות של "הזדמנויות מובילות" בקובץ החי `gsc_customer_language.md` (שלב 0). מקור יחיד לדאטת חיפוש = הקובץ החי.

> **v7.4 (יישור עם brand-hub-machine):** ארבע תוספות מעמודי המותג. (1) **שער VALIDATE אוטומטי** בשלב 13: בדיקת Python לחוסמי Elementor (CSS מאוזן, אפס unicode-bidi/SVG/U+2011/U+00AD/U+200B/data-ga-event/target, רצף %XX ארוך ב-href) לפני הגשה, במקום בדיקה ידנית בלבד. (2) **טוקנים לפי אתר**: ה-CSS מונע ממשתני `--bh-*`, הזרק `../brand-hub-machine/tokens-[site].css` לפי האתר. הבלוג תואם עכשיו ל-Design System של כל אתר (CSB כתום, מרום navy+אדום, פלרום כתום) במקום כחול גנרי. **[בוטל ב-v7.7 — בלוק ה-tokens הפיל את השמירה ב-Elementor; הוחזר ל-hex קשיח. ראה v7.7 למעלה.]** (3) **Speakable** בישות Article, מצביע על `.direct-answer`. (4) **רצועת Preferred Sources** (`bh-pref-mini`, וריאנט קומפקטי) כאלמנט אחרון אחרי author-bio, לכל אתר.
